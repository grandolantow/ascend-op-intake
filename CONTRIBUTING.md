# 贡献说明

欢迎提交聚焦于需求采集边界、证据追踪、契约结构和脚本可验证行为的改进。
请先阅读 `SKILL.md` 及相关 `references/`，保持新建、续办、只验证三条路由
和洁净室限制清晰；不要把实现、CANN 安装、NPU 运行或性能采集塞入本 skill。

修改后至少运行：

```bash
python scripts/test_intake.py -v
```

文档改动还应做 UTF-8 读回和链接/命令的人工检查；脚本或数据结构改动应补充
对应回归测试。外部官方 validator 可能需要 PyYAML，是否运行及其依赖应在
贡献说明中明确；通过它不等于真实算子或 NPU 验证。

不要提交真实模型包、权重、材料正文、凭据、采集归档或个人机器绝对路径。
示例应使用相对路径和脱敏占位内容。涉及许可证、第三方来源或技能范围的
修改，请先与维护者讨论。

维护文档时，适配依据来自 `references/design-rationale.md`：其中记录了
GPT-6 Astra 当前官方提示指南、官方技能规范和查阅日期（2026-09-14），以及
本 skill 对证据驱动追问、按需加载、最终批准后冻结和确定性校验的取舍。若
官方指导或目标模型变化，只更新受影响的文档/依据并注明来源、模型和日期；
不据此强制新增架构或静默改写用户已有契约。
