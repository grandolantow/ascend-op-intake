---
name: ascend-op-intake
description: 澄清或修订 Ascend 算子需求，选择材料并冻结接口与验收契约；不承担内核实现、环境安装或性能采集。
---

# Ascend 算子需求采集

把需求、参考实现、工作负载、基线和性能剖析材料整理为一份经用户批准的
算子契约。能够从允许材料中查明的事实自行调查；只询问会改变契约且证据
无法解决的决策。最终批准仍由用户给出。

## 路由

- 新建：阅读[工作流](references/intake-workflow.md)，初始化并盘点。
- 续办：先读取已有 `materials.json`、`operator-task.json` 和决策日志，
  只调查当前缺口或变化；需要恢复产物或冻结时再读工作流。
- 只验证：用 `scripts/validate_intake.py --materials <清单> --contract <契约>`
  做只读校验，不加 `--write-hash`；遇到具体结构问题时再读
  [契约结构](references/contract-schema.md)。

打开材料正文前阅读[材料策略](references/material-policy.md)，核对模式、选择
和可见性；用户已明确的决定直接记录沿用，不因新建或续办再次索要确认。
未决定的争议材料只盘点元数据，不先读正文。

## 推进与交付

在已授权的材料与产物范围内，连续完成调查、证据记录和草稿整理；不要在
每个步骤后等待许可。遇到一个缺口时先完成其他不依赖它的工作。
需要用户裁决时，按需阅读[追问指南](references/questioning.md)，提供证据、
建议和影响；已确认且证据未变化的事项不重问，不把建议默认值当成批准。

用中文简述本轮新增结论、证据定位和待决策项，避免每轮重发完整问卷。
完整摘要供最终审阅；用户明确批准后，按工作流记录批准、冻结两个摘要并
普通复验通过，才报告完成。等待决定或工具受限时，保存可用草稿并说明
具体阻塞、责任人或所需输入；不把等待批准、校验失败或有阻塞的草稿称为完成。

## 不可破坏的约束

- 配置值只能表示证据或决策，不能被视为删除软件包、访问网络、修改
  shell 配置、写入远端或进行其他外部变更的隐式授权。
- 在洁净室生成上下文中，绝不打开 `forbidden`、`human_review` 或
  `evaluator_only` 材料的正文；可以列出名称、大小和哈希。
- 如果禁止的实现细节已经进入生成上下文，把本次采集标记为已污染；
  事后修改可见性标签不能消除污染。
- 可写状态、别名关系、物理步长、图捕获约束、动态形状、计时边界和
  逐用例门槛都属于接口决策，而不是实现细节。
- 执行需求采集只写采集产物，不开始内核生成、编译、性能测试、环境安装
  或实现仓库修改；维护本技能文件是独立任务，不受这条产物限制误拦。
- 项目说明已经授权在当前任务内委派时，不再为委派本身重复索要授权；
  委派仍不得扩大材料可见性或外部变更范围。
- 用户指令优先于本技能的流程建议，但不覆盖更高层的权限或项目限制。
  指令与当前材料模式冲突时，先明确模式或任务范围；不得将已污染的上下文
  重新标为洁净室。因技能要求暂停时，链接具体条款并解释需要什么决定。

## 维护与验证

维护本技能时才读[适配依据](references/design-rationale.md)，核对目标模型的
当前官方指南；普通需求采集不为此联网。文案或路由修改检查触发与行为，
脚本或数据结构修改运行 `scripts/test_intake.py`，并运行 skill 校验器。
通过必要检查后，只因新修改、失败或未解决风险扩展测试。
仅复现历史案例时读 [Compressor 冒烟记录](references/deepseek-v4-smoke-test.md)；
该记录不是现场材料完整性或最终接口获批的证明。
